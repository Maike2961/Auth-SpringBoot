spring: 
    application: 
        name: user_authentication_basic
    datasource:
        url: jdbc:postgresql://localhost:5432/mydatabaseauth
        username: admin
        password: admin
        driver-class-name: org.postgresql.Driver
    jpa:
        show-sql: true
        hibernate:
            ddl-auto: update
        properties:
            hibernate.format_sql: true

